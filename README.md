# Python environment (The folder we created with venv)
venv/

# Environment variables (Where your keys will live)
.env

# RSA Private Key files (Never upload these!)
*.pem
*.key

# Compiled Python files (These are created automatically)
__pycache__/
*.pyc
*.pyo

# Windows junk files
Thumbs.db
desktop.ini